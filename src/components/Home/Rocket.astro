---
import Svg from "@jasikpark/astro-svg-loader";

const { classes } = Astro.props;
---

<div class={`rocketHolder ${classes}`}>
    <img
        class="fire"
        src="/visual/fire.gif"
        width="25"
        height="25"
        alt="fire"
    />
    <Svg
        class={"rocketSvg"}
        src={import("@assets/visual/rocket.svg?raw")}
        aria-label="Drawing of a rocket"
    />
</div>

<style>
    .rocketHolder {
        display: flex;
        align-items: center;
        justify-content: center;
        flex-direction: row;
    }

    .rocketSvg {
        z-index: 1000;
    }

    .fire {
        transform: rotate(-90deg) translateY(15%);
        z-index: 1000;
    }

    html[data-theme="dark"] .fire {
        filter: invert();
    }
</style>
